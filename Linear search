#include <stdio.h>
int main() {
    int a[] = {10, 20, 30, 40, 50};
    int key = 30, i;
    for(i = 0; i < 5; i++)
        if(a[i] == key) {
            printf("Element found at position %d", i + 1);
            break;
        }
    return 0;
}
