#include <stdio.h>
#include <string.h>

int main()
{
    char str[] = "abc123xy45";
    int count = 0;

    for (int i = 0; i < strlen(str); i++)
        if (str[i] >= '0' && str[i] <= '9')
            count++;

    printf("Digits = %d", count);

    return 0;
}