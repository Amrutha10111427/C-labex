#include <stdio.h>
#include <string.h>

int main()
{
    char str[] = "banana";
    char ch = 'a';

    for (int i = 0; i < strlen(str); i++)
        if (str[i] != ch)
            printf("%c", str[i]);

    return 0;
}