#include <stdio.h>
#include <string.h>

int main()
{
    char str[] = "Programming in C";
    char sub[] = "C";

    if (strstr(str, sub) != NULL)
        printf("Substring Found");
    else
        printf("Not Found");

    return 0;
}