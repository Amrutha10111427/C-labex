#include <stdio.h>
#include <string.h>

int main()
{
    char str[] = "banana";
    char ch = 'a';
    int count = 0;

    for (int i = 0; i < strlen(str); i++)
        if (str[i] == ch)
            count++;

    printf("Frequency = %d", count);

    return 0;
}