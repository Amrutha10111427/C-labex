#include <stdio.h>
int main() {
    int a[5] = {2,5,8,7,10};
    int count = 0, i;
    for(i = 0; i < 5; i++)
        if(a[i] % 2 == 0)
            count++;
    printf("Even numbers = %d", count);
    return 0;
}
