#include <stdio.h>
int main() {
    int a[5] = {12, 45, 23, 67, 34};
    int min = a[0], i;
    for(i = 1; i < 5; i++) {
        if(a[i] < min)
            min = a[i];
    }
    printf("Minimum = %d", min);
    return 0;
}
