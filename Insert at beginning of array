#include <stdio.h>

int main() {
    int a[6] = {20,30,40,50};
    int n = 4, i;

    for(i = n; i > 0; i--)
        a[i] = a[i-1];

    a[0] = 10;

    for(i = 0; i <= n; i++)
        printf("%d ", a[i]);

    return 0;
}
