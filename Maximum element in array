#include <stdio.h>
int main() {
    int a[5] = {12, 45, 23, 67, 34};
    int max = a[0], i;
    for(i = 1; i < 5; i++) {
        if(a[i] > max)
            max = a[i];
    }
    printf("Maximum = %d", max);
    return 0;
}
