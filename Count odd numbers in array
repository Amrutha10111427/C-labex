#include <stdio.h>

int main() {
    int a[5] = {1,2,3,4,5};
    int count = 0, i;

    for(i = 0; i < 5; i++)
        if(a[i] % 2 != 0)
            count++;

    printf("Odd Numbers = %d", count);

    return 0;
}
