#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main()
{
    char str[] = "Hello@2026!";
    int count = 0;

    for (int i = 0; i < strlen(str); i++)
        if (!isalnum((unsigned char)str[i]))
            count++;

    printf("Special = %d", count);

    return 0;
}